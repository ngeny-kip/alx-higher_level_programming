#!/usr/bin/python3

def max_integer(my_list=[]):
    '''find the max interger'''
    if len(my_list) == 0:
        return (None)

    large = my_list[0]
    for i in range(len(my_list)):
        big = my_list[i]

    return (large)
